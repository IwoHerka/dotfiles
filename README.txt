Where stuff is and how it works

Executables:

    Where?: /usr/local/bin
    How to add simple exec, example:

        #!/bin/bash
        ~/S/android-studio/bin/studio.sh

Important files:

          ~/.env
          ~/.paths
          ~/.init
          ~/.zshrc
          ~/.Xdefaults
